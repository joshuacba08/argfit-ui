{
  "name": "argfit-ui",
  "version": "0.1.0-alpha.0",
  "scripts": {
    "ng": "ng",
    "start": "pnpm build:libs && ng serve",
    "build": "ng build",
    "build:core": "ng build argfit-ui-core",
    "build:primitives": "ng build argfit-ui-primitives",
    "build:desktop": "ng build argfit-ui-desktop",
    "build:mobile": "ng build argfit-ui-mobile",
    "build:adaptive": "ng build argfit-ui-adaptive",
    "build:libs": "pnpm build:core && pnpm build:primitives && pnpm build:desktop && pnpm build:mobile && pnpm build:adaptive",
    "build:all": "pnpm build:libs && ng build showcase",
    "build:packages": "pnpm build:libs",
    "pack:alpha:dist": "node tools/pack-alpha.mjs",
    "pack:alpha:dry-run:dist": "node tools/pack-alpha.mjs --dry-run",
    "pack:alpha": "pnpm build:libs && pnpm pack:alpha:dist",
    "publish:alpha:dry-run": "pnpm build:libs && pnpm pack:alpha:dry-run:dist",
    "smoke:alpha": "node tools/alpha-smoke.mjs",
    "release:alpha:check": "pnpm guard:architecture && pnpm build:all && pnpm test:all && pnpm pack:alpha:dry-run:dist && pnpm pack:alpha:dist && pnpm smoke:alpha",
    "guard:architecture": "node tools/architecture-guard.mjs",
    "guard:regression": "node tools/regression-guard.mjs",
    "audit:accessibility": "ng test argfit-ui-primitives --watch=false && ng test argfit-ui-desktop --watch=false && ng test argfit-ui-mobile --watch=false && ng test argfit-ui-adaptive --watch=false && ng test showcase --watch=false",
    "test:all": "pnpm build:libs && pnpm guard:architecture && pnpm guard:regression && ng test argfit-ui-core --watch=false && ng test argfit-ui-primitives --watch=false && ng test argfit-ui-desktop --watch=false && ng test argfit-ui-mobile --watch=false && ng test argfit-ui-adaptive --watch=false && ng test showcase --watch=false",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "dependencies": {
    "@angular/cdk": "^21.2.11",
    "@angular/common": "^21.2.0",
    "@angular/compiler": "^21.2.0",
    "@angular/core": "^21.2.0",
    "@angular/forms": "^21.2.0",
    "@angular/platform-browser": "^21.2.0",
    "@angular/router": "^21.2.0",
    "@ionic/angular": "^8.8.7",
    "@lucide/angular": "^1.16.0",
    "@primeuix/themes": "^2.0.3",
    "@tailwindcss/postcss": "^4.3.0",
    "echarts": "^6.1.0",
    "postcss": "^8.5.14",
    "primeicons": "^7.0.0",
    "primeng": "^21.1.8",
    "rxjs": "~7.8.0",
    "tailwindcss": "^4.3.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/build": "^21.2.11",
    "@angular/cli": "^21.2.2",
    "@angular/compiler-cli": "^21.2.0",
    "jsdom": "^28.0.0",
    "ng-packagr": "^21.2.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2",
    "vitest": "^4.0.8"
  }
}
