# @argfit-ui/desktop

Desktop rendering package for ArgFit UI.

Contains desktop-oriented implementations backed by PrimeNG internally while keeping public APIs vendor-independent.

Current slice:

- `AfButtonDesktopComponent`

```bash
pnpm build:desktop
```
