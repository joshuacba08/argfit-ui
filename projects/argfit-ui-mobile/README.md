# @argfit-ui/mobile

Mobile rendering package for ArgFit UI.

Contains touch-oriented implementations that render Ionic elements internally while keeping public APIs vendor-independent.

Current slice:

- `AfButtonMobileComponent`

Consumers should initialize Ionic Angular in the host application.

```bash
pnpm build:mobile
```
