# @argfit-ui/core

Foundational, vendor-agnostic package for ArgFit UI.

Contains:

- design token names and default dark theme
- theme application service
- platform detection service
- shared public types
- `provideArgfitUi`

This package must not import PrimeNG, Ionic, desktop, mobile, or adaptive implementations.

```bash
pnpm build:core
```
